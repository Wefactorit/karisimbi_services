apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: mcp-grafana
  namespace: mcp-servers
  labels:
    app: mcp-grafana
    version: v1.0.0
    managed-by: cloudeefly
spec:
  hosts:
  - mcp-grafana.mcp-servers.svc.cluster.local
  ports:
  - number: 8000
    name: http
    protocol: HTTP
  location: MESH_INTERNAL
  resolution: DNS
