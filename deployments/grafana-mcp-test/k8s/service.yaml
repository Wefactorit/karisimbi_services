apiVersion: v1
kind: Service
metadata:
  name: grafana-mcp-test
  namespace: mcp-servers
  labels:
    app: grafana-mcp-test
    version: v1.0.0
    managed-by: cloudeefly
    mcp-server: "true"
  annotations:
    cloudeefly.io/deployment-id: "grafana-mcp-test-1760666185"
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: ClusterIP
  selector:
    app: grafana-mcp-test
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800  # 3 hours for long-running SSE connections
